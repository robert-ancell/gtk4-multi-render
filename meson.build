project('gtk4-multi-render', 'c',
        version : '0.1')

gtk_dep = dependency('gtk4')
gl_dep = dependency('gl')

exe = executable('gtk4-multi-render',
                 sources : [ 'main.c' ],
                 dependencies : [ gtk_dep, gl_dep ])
